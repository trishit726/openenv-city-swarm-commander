# Core OpenEnv framework
openenv-core==0.2.3

# Web server
fastapi>=0.100.0
uvicorn>=0.23.0

# Data validation (required by OpenEnv wire types)
pydantic>=2.0.0

# Numerical ops used in environment physics
numpy>=1.24.0

# Rendering (render() method — headless safe via MPLBACKEND=Agg)
matplotlib>=3.7.0

# LLM inference (used by inference.py / inference_v2.py)
openai>=1.0.0
